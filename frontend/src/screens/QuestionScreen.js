import React from 'react';
import FormContainer from '../components/FormContainer';
const QuestionScreen = ({ location }) => {
    return (
        <FormContainer>
            <h1 className='text-center' style={{ color: 'white' }}>
                Hoy
            </h1>
        </FormContainer>
    );
};

export default QuestionScreen;
